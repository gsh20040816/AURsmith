source /etc/makepkg.conf

# AURsmith 以 .SRCINFO 中声明的 split outputs 作为发布边界。Arch 默认开启的
# debug 选项会在未声明 split package 时额外生成 *-debug 包，因此构建时显式
# 关闭该选项；其余 Arch 默认构建选项保持不变。
OPTIONS=(strip docs !libtool !staticlibs emptydirs zipman purge !debug lto)
